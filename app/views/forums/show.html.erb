<div class="left-sidebar">
  <table> 
    <% @genres.each do |genre| %>
      <tr>
  			<td><%=link_to genre.genre_name, genre_post_path(genre) %></td>
      </tr>
    <% end %>
  </table>
</div>

<div class="shared-panel">
</div>

<div class="right-sidebar">
  <table> 
    <% @fact_types.each do |type| %>
      <tr>
  			<td><%= type.fact_name %></td>
      </tr>
    <% end %>
  </table>
</div>
