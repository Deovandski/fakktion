<h2>Forum</h2> 
<p class="frontend_index"><strong>DO NOT USE PERSONAL INFORMATION!</strong></p>
<p>This Experimental version does not use SSL! Besides, there are lots of bugs to take care of before security can be tackled.</p>
<p>Signup, login and edit user are working... However, you may have to access user edit by appending /edit at the url for now...</p>
<label class="label_forum_info">{{#link-to 'adminMessages'}}Check Admin Messages{{/link-to}}</label><br/>
<label class="label_forum_info">V: Views | C: Comments</label>
<div class="posts_info">
	<ul>
		{{#each filteredPosts as |post|}}
		<li>
			<label class="label_title">
				{{#link-to 'post' post}}{{post.title}}{{/link-to}}
			</label>
			<label class="label_views">
				V:{{post.views_count}}
			</label>
			<label class="label_comments_count">
				C:{{post.comments_count}}
			</label>
		</li>
		{{/each}}
	</ul>
</div>

{{#if session.isAuthenticated}}
	<p class="inner_info_redux">{{#link-to  'posts.create'}}Create Post{{/link-to}}</p>
{{else}}
	<p>Please login to create a post</p>
{{/if}}

{{outlet}}
