<div class="outer-container">
	<div class="branding">
		Fakktion
	</div>
	<nav>
		<ul>
			{{#if session.isAuthenticated}}
			<li>
            <p>Signed in as {{session.currentUser.displayName}}</p>
			</li>
			<li>
				<a {{ action 'invalidateSession' }}>Logout</a>
			</li>
			{{else}}
			<li>
				{{#link-to 'users.create'}}SignUp{{/link-to}}
			</li>
			<li>
				{{#link-to 'login'}}Login{{/link-to}}
			</li>
			{{/if}}
		</ul>
	</nav>
</div>
